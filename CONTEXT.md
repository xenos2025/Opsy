# Opsy — Guided Shopify Operations

面向 Shopify 初学者与兼职运营者的引导式运营系统。它以完整审计到持续运营的方法为基础，隐藏专业代理路由和配置细节，但保留证据、审批、执行与验证闭环。

## Language

**B2B 询盘站**:
以获得合格联系、报价请求、表单或 WhatsApp 询盘为主要转化目标的 Shopify 独立站，而不是以在线结账和订单履约为主要运营闭环。
_Avoid_: B2B 商城、企业官网、DTC 店铺

**运营者**:
使用 Codex 或 WorkBuddy 完成日常 Shopify 运营的人；可以不具备编程、命令行或 Agent 编排知识。
_Avoid_: 技术人员、开发者、客户

**基础运营套组**:
覆盖首次检查、任务选择、内容与商品操作、店铺写入及周期复盘的精简能力集合。“基础”表示更容易操作，不表示只读、低能力或省略安全验证。
_Avoid_: 完整审计包、内容工厂、简版报告工具

**Opsy**:
“Ops”与“easy”的品牌化组合，是 **Opsy — Guided Shopify Operations** 的统一短名。项目、主入口与已注册 Skill 均使用 `opsy`；中文名称为“Opsy Shopify 引导式运营助手”。
_Avoid_: 多个业务 Skill 名称、过长技术名称

**引导式运营**:
系统根据店铺现状一次询问或推荐一个必要步骤，自动选择内部能力，并为每项操作给出前置条件、预览、完成标准和下一步。
_Avoid_: Agent 路由、让用户自行选 Skill、报告后自行处理

**主入口**:
运营者在 Codex 或 WorkBuddy 中启动套组的唯一入口。它读取当前状态、展示可用选择，并将选中的业务目标交给内部专项能力。
_Avoid_: Runtime、总控 Agent、Skill 目录

**工作流模块**:
主入口按状态和运营者选择加载的内部业务说明、模板与脚本集合。六个工作流模块不单独注册为用户可调用 Skill。
_Avoid_: 子 Skill、业务 Agent、菜单 Skill

**运营工作区**:
由项目根目录的 `shopify-ops.json` 定位、用于保存店铺档案、月度数据快照、运营产物、操作记录与写前备份的目录。目录可以叫 `shopify-ops/` 或 `_project/`，名称本身不定义能力。
_Avoid_: `_project`、客户仓库、Skill 目录

**新建型项目**:
客户已有或新建了一个本地项目目录，但尚无运营工作区。主入口在确认后创建默认 `shopify-ops/` 与定位文件，只新增缺失内容。
_Avoid_: 空项目、首次安装

**最小工作区**:
新建型项目由构建器创建的轻量运营工作区，包含客户说明、店铺档案、月度数据、素材收件箱、运营产物、操作记录、写前备份和临时目录，但不包含 Skill 副本。
_Avoid_: 工作区模板、项目脚手架、`_project`

**仓库型项目**:
客户从 GitHub 克隆的完整运营仓库，已经包含定位文件、运营工作区，且可能同时包含 `theme/`、项目规则和历史记录。主入口验证并沿用现有结构，不重新构建或改名。
_Avoid_: 旧项目、`_project` 项目、已安装项目

**项目规则**:
项目根目录 `AGENTS.md` 中约束 AI 协作、店铺目标、主题边界、安全和数据使用的持久指令。已有项目规则保持原样；运营工作区的客户说明不替代它。
_Avoid_: Skill 说明、客户 README、全局规则

**状态菜单**:
主入口依据店铺连接、建档与操作审批状态生成的当前可用选项集合。不可执行的选项不会伪装成可用功能，运营者也可以直接用自然语言表达同一目标。
_Avoid_: 固定功能列表、Agent 选择器

**运营周报**:
以本周操作进度、待处理事项和下周任务为主体的周期交付物。月度数据快照只作辅助证据；没有覆盖本周的数据时，不生成周度流量或转化趋势结论。
_Avoid_: 周流量报告、数据周报、任务清单

**新周提示**:
按店铺时区进入新运营周后，主入口在本周第一次使用时提示生成上周运营周报的状态行为。提示不等于自动生成，运营者可以确认或暂缓。
_Avoid_: 周报定时任务、自动周报、提醒服务

**月度数据快照**:
由服务方按月推送到运营项目 `data-center/` 的标准化 GA4、GSC 或其他运营数据文件，并附带来源与时间范围清单。它是客户侧数据查询的依据，不等同于实时 Google 数据。
_Avoid_: 实时数据、Google 连接、原始导出包

**月度数据交付**:
客户通过仓库数据更新从 GitHub 拉取，或接收服务方直接发送的数据包，把月度数据快照落入同一运营工作区结构的过程。两种方式使用相同文件与来源清单契约。
_Avoid_: 实时拉取、Google 授权、数据同步

**404 候选数据集**:
月度数据中心中的选填 `gsc_not_found.csv`，保存服务方从 Search Console Page Indexing 等来源交付的失效 URL 候选及来源信息。缺少该文件不阻断 404 功能，文件中的 URL 也不能未经公开验证直接重定向。
_Avoid_: 404 清单、重定向清单、GSC 错误

**数据查询**:
基于当前有效月度数据快照回答运营问题，并始终说明数据日期范围与新鲜度。
_Avoid_: 实时查询、拉取 Google 数据

**上月摘要**:
月度数据更新成功后自动生成的一页数据概览，说明月份、来源、新鲜度、主要 GSC/GA4 变化、可用询盘证据和最多三个运营提示。有可比快照时才提供环比。
_Avoid_: 月度审计、数据报告、实时看板

**数据更新**:
接收并校验服务方推送的新月度数据快照，归档被替换的旧快照，再更新数据来源清单与当前数据状态。
_Avoid_: 刷新 Google 数据、重新授权

**仓库数据更新**:
仓库型项目从 GitHub 获取服务方交付的更新时采用的受控流程：先预览远程文件变化，只在本地干净且可快进时请求确认并拉取，随后验证月度数据快照。主题、配置或规则变化必须单独披露。
_Avoid_: `git pull`、自动同步、强制更新

**404 处理**:
验证失效 URL 的现状与价值，再选择创建重定向、修正来源链接、保留 404/410 或忽略无价值 URL 的运营流程。创建 Shopify 重定向只是其中一种需要批准的结果。
_Avoid_: 404 重定向处理、批量跳转首页、自动修复所有死链

**404 待处理队列**:
月度数据更新后自动生成的只读候选列表，合并可选数据集、上月遗留项和已知 handle 变化，并记录当前响应、来源、新鲜度与初步分类。它不是重定向写入清单。
_Avoid_: 404 清单、重定向队列、自动修复结果

**重定向批准集合**:
运营者从逐条验证且目标明确的 301 候选中主动选出的精确集合。一次批准只覆盖这些条目，不代表批准全部 404 候选。
_Avoid_: 批量重定向、全部修复、重定向队列

**商品草稿**:
已写入 Shopify Admin、但尚未作为正式商品发布到销售渠道的完整商品对象。草稿必须经过同通道回读，才能进入正式发布确认。
_Avoid_: 已上架商品、待上传商品、发布预览

**商品队列**:
一次商品运营中待处理的多个候选商品集合。每个候选商品独立收集事实、生成预览、接受校验并记录错误，运营者只选择通过项创建商品草稿。
_Avoid_: 批量上传、商品表格、批处理任务

**聊天商品输入**:
运营者直接在 Codex 或 WorkBuddy 对话中发送商品图片、视频并提供基础事实的输入方式。附件必须先整理进当前运营工作区并归属到候选商品，才能进入商品队列。
_Avoid_: 临时附件、聊天上架、图片识别上架

**商品资料文件夹**:
运营工作区中按批次和候选商品保存原始图片、视频、说明与可选表格的标准收件箱，是商品资料的默认持久入口。
_Avoid_: 上传目录、素材库、商品队列

**素材归组**:
把聊天附件或商品资料文件夹中的图片、视频和说明明确归属到一个候选商品的步骤。明确单品可自动归组；多商品或存在歧义时必须由运营者确认。
_Avoid_: 图片分类、视觉聚类、自动分商品

**元字段定义**:
建站阶段在 Shopify 中建立的对象类型、namespace、key、数据类型与验证规则。基础运营只读取并验证这些定义，不创建、修改或删除它们。
_Avoid_: 元字段、商品属性、字段配置

**元字段填写**:
依据已验证的元字段定义，为商品等 Shopify 对象写入业务值的运营操作。定义缺失或不匹配时，对应值必须停止写入并交回建站处理。
_Avoid_: 元字段管理、元字段编辑、创建字段

**文章包**:
写入 Shopify 前的完整 Blog 交付物，包含标题、handle、摘要、正文、SEO、标签、图片、内链、CTA、来源依据与待确认事实。
_Avoid_: 博客草稿、文章正文、内容方案

**数据推荐选题**:
依据最新有效月度数据快照与现有站内页面提出的 Blog 候选主题。推荐必须显示数据月份、依据和重复或关键词冲突检查结果。
_Avoid_: 自动选题、热门选题、SEO 选题

**客户指定主题**:
运营者直接提供主题、问题、素材或产品资料形成的 Blog 起点。它不要求月度数据，但不得附会搜索量、排名或机会分。
_Avoid_: 无数据选题、主观选题、临时主题

**Blog 草稿**:
文章包通过校验并经第一次批准后写入 Shopify、但尚未公开或排期发布的文章对象。草稿必须回读验证后才能进入第二次发布确认。
_Avoid_: 已发布文章、本地草稿、文章包

**已发布文章修订**:
对线上 Blog 文章生成写前备份和精确字段差异，经确认后直接更新原文章并验证公开页面的操作。handle 变化不属于普通修订。
_Avoid_: 重写文章、更新草稿、重新发布

**正式发布**:
Shopify 商品或 Blog 草稿通过回读验证后，经第二次明确批准才执行的公开状态、销售渠道或定时发布时间变更。
_Avoid_: 上架、保存、激活、写入 Shopify

**完整安装**:
Codex 或 WorkBuddy 已能加载套组，且目标店铺的 Shopify CLI Store 通道已通过身份认证与读写预检。仅复制 Skill 文件或仅检测到 CLI 命令不算完整安装。
_Avoid_: 已装 Skill、CLI 已存在

**共享 Skill 安装**:
把同一版本的 `opsy` 安装到 Codex、WorkBuddy 或两者的 Skill 根目录。安装器可以自动检测宿主，也允许显式选择；它只管理共享 Skill，不改动任何店铺运营工作区。
_Avoid_: 双宿主副本、复制项目、同步客户目录

**版本化发行**:
通过 GitHub Release 交付带明确版本号的 Skill、安装器与校验信息。升级前比较版本并展示计划，获得确认后归档旧 Skill；卸载只移除共享 Skill，保留客户工作区。
_Avoid_: 拉取最新版、覆盖安装、自动升级

**引导式 CLI 安装**:
主入口检测 Node、包管理器、Git 与 Shopify CLI；系统前置条件缺失时提供指引，前置条件满足时可在运营者确认后安装受支持的 CLI 版本。店铺授权保持交互式。
_Avoid_: 一键安装、静默安装、自动授权

**店铺连接未完成**:
套组已可启动，但目标店铺的 Shopify CLI Store 通道尚未通过认证与预检。此状态只允许业务问卷和公开站点检查，不允许其他审计或任何店铺写入。
_Avoid_: 只读模式、基础审计模式

**公开站点检查**:
仅依据公网访客能够访问的页面记录可观察事实，不读取或推断 Shopify Admin、主题源码、metafield 定义、分析数据或其他内部状态。
_Avoid_: 店铺审计、后台检查、全站审计

**轻量店铺建档**:
Shopify CLI Store 通道连接后，对自动运营所必需的店铺事实建立可复核档案，包括店铺身份、域名、主题、语言与市场、主 CTA、发布策略、对象结构及 metafield 定义。它是写入前置条件，不是完整运营审计。
_Avoid_: 全站审计、SEO 审计、初始化配置

**运营写入就绪**:
目标店铺已连接，轻量店铺建档已完成，且当前操作所依赖的店铺事实仍然有效。仅连接成功不等于运营写入就绪。
_Avoid_: 店铺已连接、CLI Ready

**店铺写入**:
任何改变 Shopify Admin、发布状态或主题状态的操作，包括商品、Blog 文章、metafield、页面和主题变更。写入必须先展示精确预览、获得明确批准，再通过同一通道回读验证。
_Avoid_: 自动上架、自动发布
