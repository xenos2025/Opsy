# Opsy — Guided Shopify Operations V1 共同理解

状态：V1 `0.1.0` 已实现、验证并建立首次发布基线

记录日期：2026-07-29

时区：Asia/Shanghai

## 项目目标

从 `D:\Shopify Operations Skill` 的完整 Shopify 运营体系中，提炼一套适合企业主、销售和基础运营的 B2B Skill。它在 Codex 与 WorkBuddy 中使用同一套能力，以一个主入口按当前状态展示选择，并通过 Shopify CLI Store 通道完成商品、Blog、已有 metafield 值和重定向等受控写入。企业主无需技术人员或 Google API。

V1 面向以联系表单、报价请求、WhatsApp 等为主要转化目标的 B2B 询盘型 Shopify 站点。这里的“基础”表示安装和操作更容易，不表示省略审批、证据、回读或安全边界。

正式名称为 **Opsy — Guided Shopify Operations**，中文名为 **Opsy Shopify 引导式运营助手**，项目、主入口与 Skill 标识统一使用 `opsy`。

Opsy 的配套独立站优势来自建站与运营之间的配置连续性，而不是功能锁定。建站阶段预先完成的元字段定义、发布对象、市场与语言约定，以及标准化数据导出和运营工作区，可以在连接、轻量建档后直接成为运营输入。其他符合 V1 范围的 Shopify 独立站仍可使用；主入口按实际配置开放能力，并把缺失项交回建站或数据交付环节，不得把缺少的配置或数据伪装成已就绪。

## 设计依据

本说明基于以下已检查输入和已确认决定：

- 完整能力源：用户指定的本地完整 Shopify Operations Skill，只作为方法和能力来源，不直接复制完整多 Agent 结构。
- 本地数据样本：一个既有客户运营项目，仅用于确认 `data-center/`、`manifest.json`、归档和现有运营产物形态；客户标识和数据未复制到本仓库。
- 仓库结构样本：一个既有 GitHub 运营仓库，仅用于确认根目录 `shopify-ops.json` 定位 `_project/` 的仓库型项目；仓库标识和客户内容未复制到本仓库。
- Shopify 官方 CLI 与 Admin GraphQL 文档，用于确认 Store 通道认证、GraphQL 执行、mutation 开关、商品与文章创建能力。
- 当前对话中逐项接受的产品范围、审批规则、安装方式和工作区兼容要求。
- 2026-08-31 确认的移植边界：优先 Runtime、Product Listing、Blog SEO/GEO；Site Foundation 只保留企业画像问卷；Ops Coach 简化为三项行动；Google 数据只接受服务方本地交付。

实现时仍须以当时的 Shopify 官方文档、CLI 帮助和真实店铺预检结果为准；本说明不把任何客户数据或认证状态假定为已存在。

## 单入口状态模型

| 当前状态 | 必须显示 | 可以执行 | 禁止执行 |
|---|---|---|---|
| Skill 可用，但店铺未连接 | 明确横幅“店铺连接未完成” | 企业画像问卷、本地 FAQ 资料整理、连接引导、项目工作区检查或构建 | 公开站点/技术审计、Admin 读取、任何店铺写入 |
| Shopify CLI 已连接，但尚未建档 | “轻量店铺建档未完成” | 读取建档所需事实、完成或更新店铺档案 | 商品、Blog、metafield、重定向等运营写入 |
| 已连接且建档有效 | “运营写入就绪”、状态摘要及逐项能力缺口 | 展示完整状态菜单；只执行 `write_capabilities` 已就绪的写入，其余能力允许本地准备或补配置 | 把基础建档就绪误当成所有能力就绪；跳过变量守卫、预览、审批、响应校验、回读或项目规则的写入 |

连接后的轻量店铺建档只收集自动运营必需的可复核事实：店铺身份、域名、主题、语言与市场、主 CTA、发布策略、对象结构和已有 metafield 定义。它不是完整店铺、SEO 或主题审计。

## 就绪状态主菜单

主入口按当前状态展示以下选择，同时接受自然语言目标：

1. 本周三件事
2. 商品运营
3. Blog 与内容
4. 404 处理
5. 导入服务方数据 / 查看已有摘要
6. 连接与企业画像

不可执行的项目必须解释缺少的前置条件，不能伪装成可用功能。

## 六项工作流契约

### 1. 本周三件事

只交付恰好三项可执行行动。每项说明负责人、目标、事实来源、截止日期、完成证据和风险；Shopify 写入必须标为需要批准。不得输出长报告、成熟度评分或待办堆积。服务方数据对 Product 等任务是可选优先级证据，对 Blog 是必需输入。

### 2. 商品运营

默认输入是运营者在聊天中发送的图片、视频和基础事实，以及工作区内的商品资料文件夹；Excel/CSV 仅作可选批量补充，不要求 JSON。

聊天附件先落入 `inbox/products/<batch>/<candidate>/`。明确单品可以自动归组；存在多个商品或归属歧义时必须确认，不能只凭视觉相似度决定。

批量处理采用商品队列：逐项收集事实、校验、显示错误，运营者选择通过项创建 Shopify 商品草稿。新商品第一次批准只创建 `DRAFT`，同通道回读成功后，第二次明确批准才允许正式发布或加入指定销售渠道。

商品默认从商家材料、图片、销售确认和企业画像开始，不要求 Google 数据。服务方已交付的本地 GSC/GA4 数据可以作为可选需求证据，但不能证明商品能力。PDP 买家向文案在第一次批准前必须通过共享决策简报和 `opsy-product-package-v1` 校验。

Word、PDF、表格、聊天或销售 FAQ 资料先以匿名编号文件落入 `inbox/faq/<batch>/`，再整理为 `config/buyer_faq.json`。配置把问题证据与答案发布资格分开：已接受、语言/范围/路由匹配的问题可影响 Product、Blog 和新站非数值化选题；只有独立通过 `content_use: eligible` 的答案才能成为公开事实。Page、Collection、FAQ Hub 路由由 Opsy 记录为服务方交接。互相冲突或隔离的 MOQ、交期、质保、认证或性能答案不得进入文案。

已有线上商品修改必须先备份、展示精确字段差异、获得一次明确批准，并在写入后回读。基础运营只能读取已有 metafield 定义并填写符合定义的值；不得创建、修改或删除定义。缺失或类型不匹配的定义转交建站处理。

### 3. Blog 与内容

选题入口包含：

- 有效本地 `data-center` 是 Blog 选题、草稿与更新的硬门；至少需要 `gsc_queries` 与 `ga4_landing_pages`。
- 企业画像、销售问题、客户主题、素材或产品资料用于细化已由数据选出的买家任务，不能替代数据门。

Blog 起草前必须先加载店铺档案中的 **content_voice（销售/专家角色）**；缺失则先问诊并写入 `store-profile.json`。角色按店配置（例如提花面料店可配置为精通开发与打样的面料销售），全行业通用 skill 不内置某一行业人设。选题之后再锁定**内容工种**（默认：场景选型 / 采购清单 / 对比 / 规格解读 / 行业或客户场景；可选：产品短名单）和真实买家场景，并按工艺检查起草：角色语气、选题契合、正文有用、配图、表格、内链与主 CTA。工种改变结构，不替换人声。不得只堆文章包字段；不得编造搜索量、KD 或机会分。缺少场景或商业落点时先补素材，不写百科式空文。

写入前先形成 `opsy-blog-package-v1` 文章包，通过共享买家决策、Blog 工艺和确定性包校验。数字指标只有服务方本地交付证据时才能出现。第一次批准创建未公开的 Shopify Blog 草稿，回读成功后，第二次明确批准才允许立即发布或排期。

已发布文章可以通过“写前备份 → 精确差异 → 批准 → 更新 → 回读和公开页检查”修订。handle 变化必须作为独立风险操作，并进入 404 处理链路。

### 4. 404 处理

404 是验证和分类流程，不等同于批量创建重定向。候选 URL 逐项归入：

- 有等价替代页面：可提出 301 重定向。
- 来源链接或 sitemap 错误：修正来源。
- 页面应消失且无替代：保留 404/410。
- 垃圾、探测或无业务价值 URL：忽略。

月度数据更新后自动刷新只读的 404 待处理队列，合并可选 `gsc_not_found.csv`、历史遗留项和已知 handle 变化。任何批量写入都只能覆盖运营者从已验证候选中选出的精确集合，并再次明确批准；不得自动跳转首页。

### 5. 导入服务方数据 / 查看已有摘要

企业主不配置 Google 密钥、OAuth、Property ID 或 Service Account。Opsy 只读取或导入服务方已放入本地工作区的数据包，沿用 `data-center/manifest.json` 和 `archive/YYYY-MM/`。没有数据包时 Product 可继续；新站若有有效空 manifest 和已接受的 Blog FAQ 问题，可走 `faq_seeded` 冷启动，否则 Blog 返回 `scoring_blocked`。

更新后校验来源、范围、时区、抓取时间、行数和列，并可生成一页上月摘要以及关键词建议队列。数据建议队列以 GSC 为需求基础；Product 候选可以在明确披露缺口后使用 GSC-only 行，`data_backed` Blog 候选必须同时关联有效的 GA4 落地页表现。`suggest-faq-topics` 另提供无数值排名的真实买家问题池，可补充数据主题或供新站冷启动。所有候选必须经过表面路由、站内重复检查、事实匹配和运营者选择，不能直接授权文案或写入。只有存在可比快照时才提供环比。自然语言数据问答必须始终显示数据日期范围与新鲜度。

`gsc_not_found.csv` 是选填数据集；缺少它不阻断其他数据或 404 工作流。

### 6. 连接与店铺档案

检测 Node、包管理器、Git 和 Shopify CLI 的存在及版本。Node 或 Git 缺失时只提供安装指引；前置条件满足时，展示一个固定、受支持的 CLI 版本，并在获得批准后安装。不得使用无提示的“最新版”升级已有 CLI。

店铺授权保持交互式，使用最小必要 scopes。认证完成后先执行只读冒烟检查，再建立轻量店铺档案。日志不得记录 token、cookie、Authorization header 或其他认证材料。

`site-foundation` 在企业主版只保留企业画像问卷。Tracking、Core Web Vitals、结构化数据、索引抓取、主题代码和其他技术报告均不提供。

## 项目结构兼容

全新项目默认创建：

```text
project/
  shopify-ops.json
  AGENTS.md                  # 仅全新项目可生成
  shopify-ops/
    README.md
    .gitignore
    config/store-profile.json
    config/buyer_faq.json
    data-center/
      manifest.json
      archive/
    inbox/
      products/
      content/
      faq/
      profile/
      data/
    outputs/
    ai-log/operations-log.md
    backups/
    tmp/
```

规则如下：

- 新建型项目使用根目录 `shopify-ops.json` 定位默认 `shopify-ops/`。
- 已有仓库优先读取定位文件并沿用现有目录，例如 `_project/`；不得重建、改名或复制 Skill 到客户项目。
- 已有 `_project/` 但缺少定位文件时，先展示计划并请求确认后补充定位文件。
- 已有 `AGENTS.md` 保持原样；全新空项目可生成；其他缺少规则的已有项目先询问。
- `outputs/`、`backups/`、`tmp/` 和素材收件箱默认忽略版本控制；`config/`、`data-center/`、`ai-log/` 和项目文档可提交。

## Skill 与分发结构

Codex 与 WorkBuddy 只注册一个实际 Skill：`opsy`。六项业务能力是 `references/workflow-*.md` 中由主入口直接加载的内部工作流，不单独注册或要求初学者选择 Agent。状态规则、确定性脚本、模板和测试各自分层，避免把所有逻辑堆进一个提示文件。

发行仓库为 `xenos2025/Opsy`，本地实现位于当前仓库根目录，包含项目文档、安装器和 `skills/opsy/`。Skill 自身不包含客户工作区副本。

项目通过 GitHub 仓库和带版本号的 GitHub Release 分发，提供 `install.ps1` 与 `install.sh`：

- 自动检测 Codex 与 WorkBuddy，也允许显式选择一个或两个宿主。
- 向所选 Skill 根目录安装同一版本。
- 安装或升级前校验版本和发行包、展示计划并确认。
- 已有 Skill 先归档再替换，不静默升级。
- 安装、升级、卸载均不得改动客户运营工作区；卸载只移除共享 Skill。

## V1 明确不包含

- 完整 Shopify、SEO、GEO、主题、广告或数据审计。
- 实时 GA4/GSC 拉取和客户侧 Google OAuth/密钥管理。
- 订单、退款、结账、客户账户、库存补货、折扣、税务、物流和广告投放。
- 创建、修改或删除 metafield 定义。
- 无预览、无批准的自动发布、自动重定向或批量写入。
- 后台常驻任务、自动定时报告或未经确认的 Git 合并。
- Tracking、Core Web Vitals、结构化数据、索引抓取、主题代码或其他技术验收报告。
- 对 DTC 店铺能力覆盖作出承诺；部分通用流程可以使用，但不属于 V1 验收范围。

## V1 验收条件

- 同一发行包可以安装到 Codex、WorkBuddy 或两者，并能显示同一主入口。
- 未连接时始终显示“店铺连接未完成”，且只能运行企业画像问卷、本地 FAQ 资料整理和连接/项目引导。
- 连接后未建档不能写入；建档有效后才显示完整菜单。
- 新项目可以安全生成最小工作区；仓库型项目沿用 `shopify-ops.json` 指向的现有结构。
- 商品和 Blog 新建均完成“本地预览 → 第一次批准建草稿 → 回读 → 第二次批准发布”。
- Product 与 Blog 的买家向文案在第一次批准前通过共享决策简报；需求数据不得作为商品或店铺主张证据。
- FAQ 资料可以整理为通过 `validate-buyer-faq` 的 `buyer-faq-v1` 配置；Product/Blog 只消费不落盘的 `faq-selection-v1` 安全结果。已接受的问题信号可进入 Product/Blog 路由和无指标冷启动，公开答案必须另过资格门；开放冲突保留问题用途但阻止答案复用。Blog/Product 包校验 FAQ item ID、语言、范围、路由、隔离状态、证据引用及允许用途，并阻止 FAQ 冷启动伪造搜索指标。
- 用户画像 HTML 工具可生成经 `validate-audience-intake` 校验的日期化证据；确认摘要复用现有 `store-profile.json`，不新增长期配置文件。
- 已有商品和文章修改均有备份、精确差异、批准和回读。
- metafield 只能填写已有定义，定义变更被阻止并转交。
- 服务方本地数据包可以校验 manifest，并生成带日期范围的一页摘要；界面不出现企业主 Google API 自助查询。
- 有合格 `gsc_queries` 时可生成带来源周期、路由建议和人工选择状态的 Product 候选；只有同时存在合格 `ga4_landing_pages` 时，候选才能进入 Blog 链路。任何候选都不会自动选题或写入。
- 404 队列保持只读，只有逐项验证、选中并批准的精确重定向集合可以写入。
- 安装日志、操作日志、备份和产物中不出现明文凭据。
- 项目规则、客户工作区和用户已有变更不被安装器、初始化器或卸载器覆盖。

## 最终确认

已确认使用 **Opsy — Guided Shopify Operations / `opsy`**，以当前仓库根目录作为本地发行仓库、`xenos2025/Opsy` 为远程仓库，并按本说明的 V1 范围进入实现。
