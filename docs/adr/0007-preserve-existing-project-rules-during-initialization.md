# 初始化时保留已有项目规则

运营项目构建器不得覆盖或自动追加已有的根目录 `AGENTS.md`，仓库型项目继续以其现有项目规则为最高优先级。全新空项目可以生成一份简短项目规则，指向主入口、`shopify-ops.json`、Shopify 写入门禁与运营工作区；已有项目缺少 `AGENTS.md` 时，必须先展示拟创建内容并取得确认。

无论项目规则是否存在，运营工作区都必须生成面向运营者的 `README.md`，用普通业务语言解释如何开始、六个菜单入口、数据新鲜度和写入审批。项目规则服务 AI 协作约束，客户说明服务人工操作，两者不能互相替代。
