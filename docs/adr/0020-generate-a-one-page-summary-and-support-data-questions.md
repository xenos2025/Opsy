# 数据更新后生成一页上月摘要并支持自由问答

月度数据更新校验成功后，系统自动生成一页上月摘要，显示数据月份、来源、更新时间、缺失项，并简要汇总 GSC 搜索表现与主要页面、GA4 渠道与落地页；询盘或 CTA 数据只有在快照实际包含时才纳入。有前一个月可比快照时才生成环比，没有历史数据时不得伪造趋势。

摘要最多给出三个运营提示，不扩展成完整 SEO 或站点审计。摘要完成后，运营者可以直接用自然语言查询当前有效快照，不需要理解或选择 CSV 文件；每个回答继续携带数据日期范围、新鲜度与限制。
