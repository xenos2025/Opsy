# 404 重定向支持逐条验证后的选择性批量批准

404 处理可以把多条已分类为 301、且替代目标明确的候选组成重定向批准集合。每条必须先验证源 URL 当前状态、目标 URL 可达性和现有 Shopify 重定向冲突，再以精确的“旧路径 → 新目标”表格展示原因；运营者主动选择条目并批准该集合，不能一键批准全部候选。

系统按条顺序写入并记录每条成功或失败。单条失败不撤销其他已经成功的条目，但必须在结果中突出显示并留在待处理队列。完成后逐条验证来源跳转与目标页面；一次批准不扩展到集合之外的 URL。
