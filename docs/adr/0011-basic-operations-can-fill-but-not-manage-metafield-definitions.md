# 基础运营只能填写已有元字段

元字段定义由建站阶段负责建立和维护，基础运营套组只读取店铺中已经存在且类型明确的定义，并在商品或其他对象预览中填写对应值。套组不得创建、修改或删除 metafield definition，也不得通过猜测类型绕过缺失定义。

轻量店铺建档必须保存已验证的元字段定义快照。定义缺失、类型不匹配或档案过期时，仅阻止受影响字段及依赖它的发布条件，并生成建站处理项；其他不依赖该字段的商品准备可以继续。建站方修复并重新建档后，基础运营才能继续填写。
