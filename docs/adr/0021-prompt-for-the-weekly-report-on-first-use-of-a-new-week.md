# 新周首次使用时提示生成运营周报

运营周按店铺档案中的时区和周一至周日计算。每个新星期第一次打开主入口时，系统检查上一个完整星期是否已有运营周报；没有时，把“生成运营周报”放在状态菜单顶部，但只有运营者确认后才生成。周报回顾上周已记录操作，并安排当前星期最多三项任务。

运营者可以随时手动生成本周进度版。套组不依赖 Codex 或 WorkBuddy 的后台定时能力，以保持两个宿主行为一致；没有操作记录时必须显示“上周无已记录操作”，不得补写虚构进展。
