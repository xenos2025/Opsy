# 商品队列逐项校验，选择性创建草稿

商品运营支持小批量队列，但每个候选商品必须独立完成事实收集、字段生成、metafield 匹配、媒体检查、重复检查和写入预览。单项失败只标记该项并保留修复信息，不阻止其他通过项继续；运营者可以从通过项中选择一个或多个，并针对明确的选择集合批准创建 Shopify 商品草稿。

选择性批量批准只覆盖草稿创建，不能覆盖草稿回读或正式发布。每个创建结果都必须独立记录 Shopify 对象 ID、字段和媒体验证状态；正式发布仍遵守单独的第二次确认和销售渠道披露。
